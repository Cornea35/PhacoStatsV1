"""Pure statistical helpers for PhacoStats (DB-agnostic).

These modules are intentionally free of FastAPI and SQLAlchemy so they can
be unit-tested and reused if the persistence layer moves to PostgreSQL.
"""
